# TODO

## Follow-Ups

- Smooth first-poll cold start behavior so newly queued jobs do not sit at
  `pending` long enough to look broken
- Expand LocalStack end-to-end coverage for more `domain/*`, `ip/*`, and
  `http/headers` routes
- Add more operational docs for repeated recovery flows like rebuilds, queue
  retries, root-owned artifacts, and full LocalStack resets
